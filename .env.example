# NEXUS AI - Environment Configuration Template
# Copy this file to .env.local and fill in your actual values
# DO NOT commit .env.local to version control!

# ============================================
# SUPABASE CONFIGURATION
# ============================================
# Get these from your Supabase project settings
# https://app.supabase.com/projects

# Your Supabase project URL
VITE_SUPABASE_URL=https://your-project.supabase.co

# Your Supabase anonymous/public API key
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# ============================================
# AI API KEYS
# ============================================

# Google Gemini API Key (Required for main AI engine)
# Get from: https://aistudio.google.com/apikey
API_KEY=your_gemini_api_key_here

# OpenAI API Key (Optional - for GPT-4 support)
# Get from: https://platform.openai.com/api-keys
VITE_OPENAI_API_KEY=sk-...

# Anthropic Claude API Key (Optional - for Claude support)
# Get from: https://console.anthropic.com/
VITE_ANTHROPIC_API_KEY=sk-ant-...

# ============================================
# STRIPE CONFIGURATION (Optional)
# ============================================
# For payment processing - currently not fully implemented

# Stripe publishable key (public, safe to expose)
# Get from: https://dashboard.stripe.com/apikeys
VITE_STRIPE_PUBLIC_KEY=pk_...

# Stripe secret key (private, never expose)
VITE_STRIPE_SECRET_KEY=sk_...

# ============================================
# APPLICATION CONFIGURATION
# ============================================

# Environment: development, staging, production
VITE_ENV=development

# ============================================
# NEXUS PRODUCTION HARDENING CONFIG
# ============================================

# Circuit breaker thresholds (prevent false positives from abort cascades)
# - FAIL_THRESHOLD: how many retryable failures before circuit opens (raise from 6 to 15)
# - FAIL_WINDOW_MS: time window for counting failures (60s recommended)
# - OPEN_MS: how long to keep circuit open before retry (15s for faster recovery)
NEXUS_CIRCUIT_FAIL_THRESHOLD=15
NEXUS_CIRCUIT_FAIL_WINDOW_MS=60000
NEXUS_CIRCUIT_OPEN_MS=15000

# Request concurrency limits (handle 1000+ concurrent users)
NEXUS_MAX_CONCURRENT_REQUESTS=80
NEXUS_MAX_QUEUED_REQUESTS=3000

# Rate limiter: token bucket @ 240 tokens/60s + 40-token burst capacity
# - These allow normal 3-5 request bursts without triggering safe-mode
NEXUS_RATE_MAX_PER_WINDOW=240
NEXUS_RATE_BURST=40
NEXUS_RATE_WINDOW_MS=60000

# API key rotation cooldown (safer key deployment)
NEXUS_API_KEY_COOLDOWN_MS=30000

# Response cache TTL (prevents stale safe-mode responses)
NEXUS_RESPONSE_CACHE_TTL_MS=45000

# Debug mode (set to true to see detailed error info)
VITE_DEBUG=false

# ============================================
# SETUP INSTRUCTIONS
# ============================================
# 
# 1. Create a Supabase project:
#    - Go to https://supabase.com
#    - Create a new project
#    - Copy the URL and API key
#
# 2. Get API keys:
#    - Google Gemini: https://aistudio.google.com/apikey
#    - OpenAI (optional): https://platform.openai.com/api-keys
#    - Anthropic (optional): https://console.anthropic.com/
#
# 3. Fill in the values above
#
# 4. Run the app:
#    npm install
#    npm run dev
#
# ============================================
# SECURITY NOTES
# ============================================
# 
# - NEVER commit .env.local to git
# - NEVER share your API keys publicly
# - Use .env.local for local development
# - Use environment variables on production servers
# - Rotate keys regularly
# - Use read-only keys when possible
# - Monitor API usage for unusual activity
#
